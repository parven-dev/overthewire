# Bandit level 10 -> 11

## The password for the next level is stored in the file data.txt, which contains base64 encoded data

## Approach 
    -- use cat to print value
    -- user base64 -- decode command to decode 

## Command 
 cat data.txt | base64 --decode 

